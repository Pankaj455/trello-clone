import React from "react";

const Fallback = () => {
  return (
    <p className="not-for-mobiles">
      Crello is not compatible for mobile devices. Please explore it on tablets,
      laptops or desktops.
    </p>
  );
};

export default Fallback;
